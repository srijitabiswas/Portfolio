# URL of your backend API (the "backend" folder, not this frontend).
# Local development — match whatever port your backend's `npm run dev` prints:
VITE_API_URL=https://portfolio-etd2.onrender.com/api

# Once deployed (Render/Railway/etc), change this to your live backend URL:
# VITE_API_URL=https://your-backend.onrender.com/api
